<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <style>
        table,th,td{
            border:2px solid black;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>ID</th>
            <th>Username</th>
            <th>Email Id</th>
        </tr>
        <% for(i of data){%>
            <tr>
                <td><%=i.id%></td>
                <td><%=i.username%></td>
                <td><%=i.email%></td>
                <td>
                    <form method="GET" action="/users/<%=i.id%>/edit">
                        <button>Edit Username</button> 
                    </form>
                </td>
                <td>
                    <form method="GET" action="/users/<%=i.id%>/delete">
                        <button>Delete User</button>
                    </form>
                </td>
            </tr>
        <%}%>
    </table>
</body>
</html>